import { View, StyleSheet, Text } from "react-native";

export default function Onboarding() {
    return (
        <View style={styles.container}>
            <Text>daksodaskdokosa</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
    }
})